[project]
name = "tuya-ble-access-development"
version = "0.2.2"
requires-python = ">=3.13"
dependencies = []

[dependency-groups]
dev = [
  "pytest>=8,<10",
  "PyYAML>=6,<7",
  "cryptography>=41",
  "bleak>=0.21",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = [".", "tests"]

[tool.uv]
package = false
